apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n-redis
  namespace: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: n8n-redis
  template:
    metadata:
      labels:
        app: n8n-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: n8n-redis-master
  namespace: production
spec:
  selector:
    app: n8n-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
