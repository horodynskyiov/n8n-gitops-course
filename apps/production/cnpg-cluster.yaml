apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: n8n-db
  namespace: production
spec:
  instances: 2

  storage:
    size: 5Gi
    storageClass: local-path

  superuserSecret:
    name: n8n-db-superuser

  bootstrap:
    initdb:
      database: n8n
      owner: n8n
      secret:
        name: n8n-db-app
